### heap.py

```py
#获取从节点node往下的最大元素
def maxHeapify(self,node):
  pass
  
#建堆函数：
#唯一不懂的地方是：
#为什么要倒着建堆？
def buildHeap(self,a):
  pass
  
#寻找堆中的最大元素：
#即，堆顶元素就为最大元素，如果堆顶元素存在，返回堆顶元素
#然后将堆的最后一个元素与堆顶元素交换
#对堆进行建立，排除之前堆顶元素
def getMax(self):  
  pass

#堆排序
#将堆尾元素移到堆顶，
#然后对 除 堆尾元素 之外的数组，进行排序
def heapSort(self):
  pass
  
#向堆中插入一个元素
#
def insert(self,data):  
  pass
  










```
